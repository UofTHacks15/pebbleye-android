<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uofthack.pebbleye" >

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:theme="@style/CustomTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:parentActivityName="com.uofthack.pebbleye.MainActivity"
                android:value="com.uofthack.pebbleye.MainActivity" />
        </activity>
        <activity
            android:name=".GeocodingActivity"
            android:label="@string/title_activity_geocoding"
            android:parentActivityName=".MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:parentActivityName="com.uofthack.pebbleye.MainActivity"
                android:value="com.uofthack.pebbleye.MainActivity" />
        </activity>
    </application>

</manifest>
